@using Umbraco.Cms.Web.Common.PublishedModels
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@{
    Layout = "Master.cshtml";
}
<h1>@Model.Value("title")</h1>

@if (Model.HasValue("description"))
{
    <p class="lead">@Model.Value("description")</p>
}

<div class="card">
    <h2>Product Details</h2>
    <dl>
        @if (Model.HasValue("serialNumber"))
        {
            <dt>Serial Number</dt>
            <dd>@Model.Value("serialNumber")</dd>
        }
        @if (Model.HasValue("sku"))
        {
            <dt>SKU</dt>
            <dd>@Model.Value("sku")</dd>
        }
        @if (Model.HasValue("color"))
        {
            <dt>Colour</dt>
            <dd>@Model.Value("color")</dd>
        }
        @if (Model.HasValue("weight"))
        {
            <dt>Weight</dt>
            <dd>@Model.Value("weight")</dd>
        }
    </dl>
</div>

@if (Model.HasValue("bodyText"))
{
    <div class="card">
        @Model.Value("bodyText")
    </div>
}
