@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<MusicEventPage>
@{
    Layout = "Master.cshtml";
}
<h1>@Model.Title</h1>

@if (!string.IsNullOrEmpty(Model.Description))
{
    <p class="lead">@Model.Description</p>
}

<div class="card">
    <h2>Event Details</h2>
    <dl>
        @if (!string.IsNullOrEmpty(Model.Performer))
        {
            <dt>Performer</dt>
            <dd>@Model.Performer</dd>
        }
        @if (!string.IsNullOrEmpty(Model.StartDate))
        {
            <dt>Start Date</dt>
            <dd>@(((DateTime)Model.StartDate).ToString("d MMMM yyyy 'at' HH:mm"))</dd>
        }
        @if (!string.IsNullOrEmpty(Model.EndDate))
        {
            <dt>End Date</dt>
            <dd>@(((DateTime)Model.EndDate).ToString("d MMMM yyyy 'at' HH:mm"))</dd>
        }
        @if (!string.IsNullOrEmpty(Model.LocationName))
        {
            <dt>Venue</dt>
            <dd>@Model.LocationName</dd>
        }
        @if (!string.IsNullOrEmpty(Model.LocationAddress))
        {
            <dt>Address</dt>
            <dd>@Model.LocationAddress</dd>
        }
    </dl>
</div>
