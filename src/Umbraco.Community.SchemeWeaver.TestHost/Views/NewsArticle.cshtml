@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<NewsArticle>
@{
    Layout = "Master.cshtml";
}
<article>
    <h1>@Model.Title</h1>
    <div class="article-meta">
        @if (!string.IsNullOrEmpty(Model.Dateline))
        {
            <span>@Model.Dateline</span>
        }
        @if (!string.IsNullOrEmpty(Model.AuthorName))
        {
            <span> &middot; By @Model.AuthorName</span>
        }
        @if (!string.IsNullOrEmpty(Model.PublishDate))
        {
            <span> &middot; @(((DateTime)Model.PublishDate).ToString("d MMMM yyyy"))</span>
        }
    </div>
    @if (!string.IsNullOrEmpty(Model.FeaturedImage))
    {
        var imageUrl = Model.FeaturedImage?.MediaUrl();
        if (!string.IsNullOrEmpty(imageUrl))
        {
            <img src="@imageUrl" alt="@Model.Title" class="featured-image" />
        }
    }
    @if (!string.IsNullOrEmpty(Model.Description))
    {
        <p class="lead">@Model.Description</p>
    }
    @if (!string.IsNullOrEmpty(Model.BodyText))
    {
        <div class="card">
            @Model.BodyText
        </div>
    }
    @if (!string.IsNullOrEmpty(Model.Keywords))
    {
        <div class="tag-list">
            @foreach (var keyword in Model.Keywords.ToString().Split(',', System.StringSplitOptions.TrimEntries | System.StringSplitOptions.RemoveEmptyEntries))
            {
                <span class="tag">@keyword</span>
            }
        </div>
    }
</article>