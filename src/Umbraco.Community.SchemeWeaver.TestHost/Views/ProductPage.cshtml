@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ProductPage>
@{
    Layout = "Master.cshtml";
}
<h1>@Model.Title</h1>
@if (!string.IsNullOrEmpty(Model.Description))
{
    <p class="lead">@Model.Description</p>
}

@if (Model.HeroImage != null)
{
    <img src="@Model.HeroImage.MediaUrl()" alt="@Model.HeroImage.Name" class="featured-image" />
}

<div class="card">
    <dl>
        @if (!string.IsNullOrEmpty(Model.Price))
        {
            <dt>Price</dt>
            <dd>@Model.Price @Model.Currency</dd>
        }
        @if (!string.IsNullOrEmpty(Model.Sku))
        {
            <dt>SKU</dt>
            <dd>@Model.Sku</dd>
        }
        @if (!string.IsNullOrEmpty(Model.Brand))
        {
            <dt>Brand</dt>
            <dd>@Model.Brand</dd>
        }
        @if (!string.IsNullOrEmpty(Model.Availability))
        {
            <dt>Availability</dt>
            <dd>@Model.Availability</dd>
        }
    </dl>
</div>

@if (Model.BodyText is not null)
{
    <div class="card">
        @Model.BodyText
    </div>
}

@if (Model.Reviews?.Any() == true)
{
    <h2>Customer Reviews</h2>
    @foreach (var block in Model.Reviews)
    {
        if (block.Content is not ReviewItem review) { continue; }
        <div class="card review-card">
            <strong>@review.ReviewAuthor</strong>
            <span> &middot; Rating: @review.RatingValue/5</span>
            @if (review.ReviewDate != default(DateTime))
            {
                <span> &middot; @(review.ReviewDate.ToString("d MMMM yyyy"))</span>
            }
            <p>@review.ReviewBody</p>
        </div>
    }
}