@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<SchoolPage>
@{
    Layout = "Master.cshtml";
}
<h1>@Model.Title</h1>

@if (!string.IsNullOrEmpty(Model.Description))
{
    <p class="lead">@Model.Description</p>
}

<div class="card">
    <h2>School Details</h2>
    <dl>
        @if (!string.IsNullOrEmpty(Model.Telephone))
        {
            <dt>Telephone</dt>
            <dd>@Model.Telephone</dd>
        }
    </dl>
</div>

<div class="card">
    <h2>Address</h2>
    <dl>
        @if (!string.IsNullOrEmpty(Model.StreetAddress))
        {
            <dt>Street</dt>
            <dd>@Model.StreetAddress</dd>
        }
        @if (!string.IsNullOrEmpty(Model.AddressLocality))
        {
            <dt>Locality</dt>
            <dd>@Model.AddressLocality</dd>
        }
    </dl>
</div>