@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<LodgingReservationPage>
@{
    Layout = "Master.cshtml";
}
<h1>@Model.Title</h1>

@if (!string.IsNullOrEmpty(Model.Description))
{
    <p class="lead">@Model.Description</p>
}

<div class="card">
    <h2>Reservation Details</h2>
    <dl>
        @if (!string.IsNullOrEmpty(Model.ReservationId))
        {
            <dt>Reservation ID</dt>
            <dd>@Model.ReservationId</dd>
        }
        @if (!string.IsNullOrEmpty(Model.CheckinTime))
        {
            <dt>Check-in</dt>
            <dd>@Model.CheckinTime</dd>
        }
        @if (!string.IsNullOrEmpty(Model.CheckoutTime))
        {
            <dt>Check-out</dt>
            <dd>@Model.CheckoutTime</dd>
        }
    </dl>
</div>
