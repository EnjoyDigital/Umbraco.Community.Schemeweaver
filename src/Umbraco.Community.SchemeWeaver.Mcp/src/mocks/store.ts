/**
 * In-memory Mock Data Store
 *
 * Backing store for MSW handlers. Reset between tests via resetStore().
 * Currently empty — populate alongside handlers.ts if mock-mode unit tests
 * are added.
 */

export function resetStore(): void {
  // No mock state yet
}
